#include "parser.h"

using namespace std;

vector<string> parse(string s, TYPE t , SOURCE src){
    switch(src){
        Frdic:
            return parse_frdic(s, t);
            break;
        default:
            return parse_frdic(s, t);
    }
}
